[project]
name = "dora-position-merge-v1"
version = "0.1.0"
dependencies = []

[project.scripts]
dora-position-merge-v1 = "dora_position_merge_v1.main:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
